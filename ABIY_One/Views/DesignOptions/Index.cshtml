@model IEnumerable<ABIY_One.Models.DesignOption>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.IncludeDesign)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.IncludeDesign)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.OptionId }) |
            @Html.ActionLink("Details", "Details", new { id=item.OptionId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.OptionId })
        </td>
    </tr>
}

</table>
